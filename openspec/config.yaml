# SDD Configuration — OHM
# Auto-generated by sdd-init on 2026-07-28

context: >
  OHM (Orchestrator & Harness for Models) is an enterprise-grade
  framework for building, deploying, and operating AI agents.
  Provider-agnostic, OS-agnostic, with Textual TUI. Uses strands-agents
  for LLM execution, pydantic-ai for structured output, MCP for tool
  integration. Package structure: src/ohm/{cli,commands,core,utils}.

strict_tdd: true

testing:
  runner: pytest
  command: uv run pytest
  layers:
    unit: pytest
    integration: false
    e2e: false
  coverage_available: false
  coverage_command: ""
  linter: ""
  type_checker: ""
  formatter: ""

persistence: hybrid

# Phase rules — inherits from SDD skill contracts
phases:
  explore: {}
  propose: {}
  spec: {}
  design: {}
  tasks: {}
  apply: {}
  verify: {}
  archive: {}
